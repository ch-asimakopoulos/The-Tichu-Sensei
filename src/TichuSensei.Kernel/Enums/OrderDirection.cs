namespace TichuSensei.Kernel.Enums
{
    /// <summary>
    /// This enum determines if the request results will be ordered in an ascending or descending order of a specified key.
    /// </summary>
    public enum OrderDirection
    {
        Ascending,
        Descending
    }
}
